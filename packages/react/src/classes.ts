/*
  The class contract lives in the core so every adapter reads it the same way;
  this file is the name React callers have always imported it under.
*/
export { createClasses, cx } from "@trapezium/core"
export type { ClassResolver } from "@trapezium/core"
